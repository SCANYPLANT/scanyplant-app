import { DefaultTheme } from 'react-native-paper';

const rootThemeStyles = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#62b2f0',
        accent: 'white',
        color: 'white',
    },
};
export default rootThemeStyles;
